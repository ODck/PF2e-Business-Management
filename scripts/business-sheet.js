// business-sheet.js

class BusinessSheet extends ActorSheet {
    constructor(...args) {
        super(...args);
    }

    // Add code for rendering and handling interactions on the business actor sheet
}